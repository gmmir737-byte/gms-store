This folder previously contained Supabase OTP edge functions, but the app now uses Supabase built-in passwordless authentication instead.

If you want to restore custom OTP functions in the future, keep these files as a reference for service-role-based OTP and brute-force protection.
